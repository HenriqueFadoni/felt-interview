import { StyleSheet } from 'react-native';

import { varColors } from '../../../style/variables.style';

export const style = StyleSheet.create({
  line: {
    height: 1,
    width: '100%',
    marginBottom: 20,
    backgroundColor: varColors.grey
  },
});
