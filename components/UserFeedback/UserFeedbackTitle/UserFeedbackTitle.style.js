import { StyleSheet } from 'react-native';

import { varColors } from '../../../style/variables.style';

export const style = StyleSheet.create({
  title: {
    textAlign: 'center',
    fontWeight: 'bold',
    color: varColors.grey
  }
});
